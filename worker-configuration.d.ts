// After adding bindings to `wrangler.toml`, 
// Regenerate this interface via `bun cf-typegen` or `wrangler typegen`.
// Or Insert type definitions here manually.
interface Env {
    MY_VARIABLE: string;
    MY_SECRET: string;
}
